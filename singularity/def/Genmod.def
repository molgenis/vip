Bootstrap: localimage
From: sif/build/Python.sif

%help
    Genmod is a tool for annotating and analyzing primarily genetic models in the VCF file format.
    Usage: genmod --help

%files
    ./def/Genmod_requirements.txt Genmod_requirements.txt

%post
  apk update
  apk add py3-setuptools jpeg zlib
  apk add --virtual=.build-dependencies build-base py3-pip jpeg-dev zlib-dev python3-dev

  pip install -r Genmod_requirements.txt
  pip install genmod==3.7.4

# cleanup
    apk del .build-dependencies
    rm -rf /var/cache/apk/* Genmod_requirements.txt
